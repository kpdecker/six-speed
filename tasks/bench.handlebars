<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"> 
  </head>
  <body>
    {{#each scripts}}
      <script src="{{.}}" {{#if ../jsType}}type="{{../../jsType}}"{{/if}}></script>
    {{/each}}
  </body>
</html>
